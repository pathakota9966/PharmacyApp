import React from 'react';
import { View, StyleSheet } from 'react-native';

const ListSeperator = () => <View style={styles.container} />;

export default ListSeperator;

const styles = StyleSheet.create({
    container: {
        height: 1,
        width: '100%',
        backgroundColor: '#C0C0C0'
    }
});